package net.phobot.realestate.contexts.closing.roles

import net.phobot.realestate.contexts.closing._

class ClosingAgent(val entityId: Long) {
  //def represents : Option[TitleCompany] = { Repository.representedBy(this) }
}
